{
  "name": "@squonk/squonk-frontend",
  "version": "0.0.0",
  "description": "Monorepo for all Squonk UI and JS projects that need to be locally developed together",
  "author": "Informatics Matters",
  "license": "Apache-2.0",
  "scripts": {
    "c:link": "pnpm c set link-workspace-packages=true prefer-frozen-lockfile=false -L project",
    "c:remote": "pnpm c set link-workspace-packages=false prefer-frozen-lockfile=false -L project",
    "c:reset": "pnpm c delete link-workspace-packages prefer-frozen-lockfile -L project",
    "i:link": "pnpm c:link && pnpm i && pnpm c:reset",
    "i:remote": "pnpm c:remote && pnpm i && pnpm c:reset",
    "dmui:dev": "MONOREPO=true pnpm --filter data-manager-ui dev",
    "dmui:build": "MONOREPO=true pnpm --filter data-manager-ui build",
    "theme:build": "MONOREPO=true pnpm --filter mui-theme build",
    "pv:dev": "FAST_REFRESH=false pnpm --filter pose-viewer dev",
    "pv:build": "pnpm --filter pose-viewer build",
    "rsc:build": "pnpm --filter react-sci-components build",
    "rsc:watch": "pnpm --filter react-sci-components start",
    "build": "pnpm build"
  }
}
